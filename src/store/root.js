const state = {
  config: {},
  version: process.env.VERSION || "0.0.1",
  initialized: false
};

const getters = {};

const actions = {};

const mutations = {};

export const RootModule = {
  state,
  getters,
  actions,
  mutations
};
